<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="google-site-verification" content="kVK4n2UxORHkEt0uBIOV7HvBKH20dyDSZz6lkbSU90M" />
    <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css"
    integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N"
    crossorigin="anonymous"
  />
  <link rel="stylesheet" href="./css/light.css">
  <style>
    .bac{
        display: flex !important;
        align-items: center;
    }
            /* Define la animación */
            @keyframes fadeIn {
            from {
                opacity: 0;
                /* Elemento invisible al principio */
                transform: translateX(100px);
                /* Puede ajustarse según la dirección deseada */
            }

            to {
                opacity: 1;
                /* Elemento completamente visible al final */
                transform: translateX(0);
                /* Sin transformación al final */
            }
        }


        /* Aplica la animación al elemento */
        .fade {
            animation: fadeIn 0.3s ease-out forwards;
            /* Nombre de la animación, duración, y función de temporización */
        }
  </style>
    <title>Bienvenido</title>
</head>
<body>
    <% if(id){ %>
        <div class="container fade">
            <br>
            <div class="bac">
                <a href="/dashboard?id=<%= id %>"><ion-icon name="arrow-round-back" class="btn-dark" size="large"></ion-icon></a>
            </div>
            <br>
            <center>
                <h1 class="display-4">Perfil</h1>
                <hr class="fader">
            </center>
            <br><br>
            <div class="alert alert-dark" role="alert">
                <center>
                    <span><ion-icon name="<%= sexo %>" size="large"></ion-icon></span>
                </center>
            </div>
            <div class="alert alert-dark" role="alert">
                <span class="btn"><ion-icon name="key" size="large"></ion-icon></span>
                <span style="margin-left: 2px;"><b>ID de usuario:</b> <%= id %></span>
                <br>
                <a class="btn"><ion-icon name="contact" size="large"></ion-icon></a>
                <span style="margin-left: 2px;"><b>Nombre:</b> <%= usuario %></span>
                <br>
                <a class="btn"><ion-icon name="call" size="large"></ion-icon></a>
                <span style="margin-left: 2px;"><b>Telefono:</b> +57 <%= phone %></span>
                <br>
                <a class="btn"><ion-icon size="large" name="star"></ion-icon></a>
                <span style="margin-left: 2px;"><b>Rango:</b> <%= rango %></span>
            </div>
            <div class="alert alert-dark" role="alert">
                <form action="/dashboard/datos?id=<%= id %>&usuario=<%= usuario %>&phone=<%= phone %>&rango=<%= rango %>" method="POST">
                    <label><b>Codigo:</b></label>
                    <input type="number" class="form-control" name="code" id="code">
                    <% if(msg == "codigoincorrecto") { %>
                        <div class="alert alert-danger" role="alert">
                            <strong class="text-black-50">El codigo no existe o ya fue utilizado</strong>
                        </div>
                    <% } %>
                    <br>
                    <button type="submit" class="btn btn-dark">Enviar</button>
                </form>
            </div>
        </div>
    <% } else {%>
        <h1>ha ocurrido un error con la sessión</h1>
        <a href="/auth">Volver a iniciar sesion</a>
    <% } %>
    <script src="https://unpkg.com/ionicons@4.5.10-0/dist/ionicons.js"></script>
</body>
</html>