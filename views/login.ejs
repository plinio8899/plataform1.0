<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css"
    integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N"
    crossorigin="anonymous"
  />
  <style>
            /* Define la animación */
            @keyframes fadeIn {
            from {
                opacity: 0;
                /* Elemento invisible al principio */
                transform: translateY(-100px);
                /* Puede ajustarse según la dirección deseada */
            }

            to {
                opacity: 1;
                /* Elemento completamente visible al final */
                transform: translateY(0);
                /* Sin transformación al final */
            }
        }

        /* Aplica la animación al elemento */
        .fade {
            animation: fadeIn 0.3s ease-out forwards;
            /* Nombre de la animación, duración, y función de temporización */
        }
  </style>
    <title>Inicia sesion</title>
</head>
<body>
    <% if(id) {%>
        <h1>ha ocurrido un error con la sessión</h1>
        <a href="/auth">Volver a iniciar sesion</a>
    <% } else{%>
        <div class="container fade">
            <br>
            <a href="/"><ion-icon name="arrow-round-back" class="btn-dark" size="large"></ion-icon></a>
            <center>
                <h1 class="display-4">Ingresa tus credenciales <%= id %></h1>
                <hr>
            </center>
            <br><br>
            <div class="alert alert-dark" role="alert">
                <form action="/auth" method="POST">
                    <label for="phone">Numero de telefono: </label>
                    <input type="number" name="phone" class="form-control" autocomplete="off">
                    <br>
                    <label for="pass">Contraseña: </label>
                    <input type="password" name="pass" autocomplete="off" class="form-control">
                    <br>
                    <% if(err != "init") { %>
                        <div class="<%= clas %>" role="alert">
                            <strong class="text-black-50"><%= err %></strong>
                        </div>
                    <% } %>
                    <center>
                        <button type="submit" class="btn btn-dark">Iniciar Sesión</button>
                    </center>
                </form>
            </div>  
            <script>
              $(".alert").alert();
            </script>
        </div>
    <% } %>
    <script src="https://unpkg.com/ionicons@4.5.10-0/dist/ionicons.js"></script>
</body>
</html>